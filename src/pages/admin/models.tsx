import { ModelsContent } from "@/components/content/admin";

type AdminModelsPageProps = {};

const AdminModelsPage = ({}: AdminModelsPageProps) => {
  return <ModelsContent />;
};

export default AdminModelsPage;
